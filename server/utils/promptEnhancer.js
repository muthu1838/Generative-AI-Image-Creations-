export function enhancePrompt(prompt) {
  return `
Ultra realistic commercial photography,
cinematic lighting, shallow depth of field,
high detail, professional advertising style,
${prompt}
`;
}
